# Assignment 7: Threat Intelligence

The individual PCAP files are stored in the `A7/pcaps/` directory. To facilitate analysis, they are merged into a single file using the following command:

```bash
mergecap -w merged.pcap pcaps/dataset_*
```

The resulting merged capture is saved as `A7/merged.pcap`.
